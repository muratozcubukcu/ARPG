#include "position.h"

// This file is mostly empty since Position is implemented in the header
// But we need it for the build system to work properly

// If you want to move implementations here later, you can do so
